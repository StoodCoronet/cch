# CCH Web 控制台 + node-ccd 需求对齐 Spec（定稿）

> 访谈日期：2026-08-06。本文档是与用户对齐后的开发依据。

## 1. 目标（Goals）

- 单位是 **conversation**：与 claude code `/resume` 可恢复的会话一一对应（`~/.claude/projects/<path>/<sessionId>.jsonl` 的 sessionId 为唯一键）。
- web 控制台能**实时看到** host 上 node-ccd 启动的 claude conversation，并能**远程控制**它干活：注入 query、授予工具权限。
- web 上下达的控制实时作用到 host 的 claude 会话；claude 的最新输出实时回到 web。
- 本地体验保留：node-ccd 本地打开 TUI 选 API profile，用选中的 key 启动 claude code；离开 host 后 web 继续接管同一会话。
- web 会话显示名：被 `/rename` 过的用 rename 结果；否则用首条用户消息摘要 + 设备名。

## 2. 非目标（Non-goals，本轮不做）

- 多会话并发管理 UI（架构预留，本轮单会话链路打磨）。
- web 远程冷启动（指定 cwd + profile + resume id 唤醒历史会话）——下轮做，本轮 daemon 协议预留命令通道。
- 验证器 / 两步验证等高级安全机制。
- Rust ccd 的继续投入（infra 方向转向 node-ccd，rust ccd 维持现状）。

## 3. 架构决策（含理由）

| 决策 | 结论 | 理由 |
|------|------|------|
| 渲染路线 | **xterm.js 真终端**，会话详情页 = 全屏终端 + 状态栏，废弃消息列表视图 | 复刻度 100%（/resume 菜单、ctrl+o、权限提示），远程控制 = 远程键盘，双向天然一致 |
| 控制通道 | node-ccd 用 **PTY** 启动 claude；web 按键经 server 中继写入 PTY stdin | 唯一能让"本地 TUI 和 web 看到同一个画面"的方案；权限批准等交互零额外开发 |
| 进程形态 | **daemon + 每会话子进程**：`ccd start` 常驻 daemon（连 server）；`ccd` 默认进 TUI 选 API，选完让 daemon spawn claude PTY 并 attach 本地终端；`ccd stop/status/edit/connect` | web 冷启动需要常驻接收方；子进程崩溃不拖 daemon |
| 本地 TUI 退出 | **detach 保留**，子进程留在 daemon，可重新 attach，web 不断线 | "离开 host 后 web 继续用"的前提 |
| 断线语义 | web 显示离线标记；恢复后自动重连续传 | 用户明确要求 |
| 安全 | 本轮：server 用户+密码登录；connect 用 token。远程控制风险（≈远程执行命令）接受现状，加强版以后做 | 用户拍板 |
| Session 创建 | 不再启动即建空 session；跟随 claude sessionId，有真实内容才出现在列表 | 解决 0 msgs 空会话占位 |

## 4. 组件改动（真实文件路径）

### node-ccd（`cli/node-ccd/`）
- `daemon.js`（新）：常驻进程。Socket.IO 连 server；管理会话子进程表（sessionId → {pty, cwd, profile, claudeSessionId, title}）；接收 server 下发的 `term-input` 写入对应 PTY；把 PTY 输出推给 server。
- `session.js`（新）：spawn claude PTY 子进程（注入 profile 的 env：`ANTHROPIC_API_KEY`/`ANTHROPIC_BASE_URL` 等）；watch `~/.claude/projects/<cwd-slug>/<sessionId>.jsonl` 提取 sessionId、title（/rename）、首条消息摘要。
- `index.js`（改）：命令分发——无参= TUI 选 profile 后请求 daemon 开会话并 attach；`connect`/`edit`/`start`/`stop`/`status`。
- `tui.js`（改）：保持与 rust ccd 一致的交互（Tab/1/2/3、↑↓/jk 不循环、Enter 启动）。
- IPC：daemon 监听 `~/.ccd/daemon.sock`（unix socket），CLI 命令与其通信。

### server（`server/`）
- `sources/app/api/socket/`：新增终端中继 namespace/handler——`term-join`(sessionId)、`term-input`(data)、`term-output`(data)、`term-resize`(cols,rows)；host 侧标记 `clientType: 'machine'`，web 侧按账号鉴权。
- `sources/app/api/routes/sessionRoutes.ts`：session 记录增加 `claudeSessionId`、`title`、`cwd`、`deviceName`、`online` 字段（无迁移权限，优先存 metadata JSON）。
- `server/user.html` / `server/user.js`：会话详情页替换为 xterm.js（CDN 引入 `xterm` + `xterm-addon-fit`）；sidebar 保留会话列表（显示 title + 设备名 + 在线点）。

### wire（`packages/wire/`）
- 终端中继消息的 zod schema（如需要类型化）。

## 5. 数据/控制流

```
web (xterm.js) ──term-input──▶ server ──▶ node-ccd daemon ──▶ PTY stdin ──▶ claude
web (xterm.js) ◀─term-output── server ◀── node-ccd daemon ◀── PTY stdout ◀─┘
```

- 鉴权：web 端 socket auth = 账号 token；daemon 端 = connect token（现有 bootstrap）。
- 离线：daemon 断开 → server 标记该设备会话 offline，web 状态栏显示；重连后 daemon 重发会话清单，web 重新 join。

## 6.  Rollout 步骤

1. node-ccd daemon + PTY 会话子进程（本地可跑通 TUI→claude）。
2. server 终端中继 + web xterm.js 会话页。
3. sessionId/title 对齐与命名规则。
4. 离线标记 + 自动重连。
5. 端到端自测脚本（起 server、起 daemon、模拟 web 按键、断言回显）。

## 7. 开放风险

- **node-pty 原生模块**：macOS 预编译二进制安装成功（agent 已验证）；`script -q /dev/null` 兜底已写未实测，且为 macOS/BSD 语法，Linux 不兼容。
- **claude `/rename` 的持久化位置**未验证：jsonl 里有 `type:'summary'` 行会用作 title；否则用首条用户消息前 60 字符。
- **多端同时输入**：本地 TUI 和 web 同时打字会互相穿插（同一 PTY），接受此行为（和多人共用 tmux 一样）。
- **PTY 尺寸**：web 端 resize 与本地终端尺寸不一致时以最后 resize 者为准。
- **`/clear` 或 TUI 内 `/resume` 换会话**：daemon 只更新 meta.claudeSessionId 并 PATCH tag，不新建 server session（同一块终端屏幕继续）。如需严格按 conversation 切分 server session，下轮处理。
- **daemon 重启不恢复会话**：PTY 子进程随 daemon 退出，会话表在内存中；web 上看到 offline/exited，需重新 spawn 或 resume。
- **多副本部署**：终端中继的 input/resize 转发只查本进程 socket（standalone OK）；Redis adapter 多副本时会丢，需 sticky 或跨进程转发。

## 8. 实施状态（2026-08-06 下午）

已全部落地并通过 e2e（`cli/node-ccd/test-e2e.js`，10/10；含离线广播场景 8/8）：

- node-ccd daemon 架构：`daemon.js` / `session.js` / `attach.js` / 重写 `index.js`（connect/start/stop/status/ls/attach/spawn + 默认 TUI 流程），node-pty 后端。
- server `terminalHandler.ts`：term:register/output/state/meta、term:join/input/resize、256KB 滚动缓冲回放、daemon 断线广播 offline。
- web 会话页 = xterm.js 终端（状态栏 + 在线点 + 重连按钮），断线重连自动 rejoin 并回放 scrollback。
- session 身份：spawn 即建 server session（pending tag 防同 cwd 冲突）；首条消息后 watcher 发现 claudeSessionId → term:meta + PATCH tag 对齐（`server/sources/app/api/routes/sessionRoutes.ts` 新增 PATCH `/v1/sessions/:id/tag`）。
- 本地开发 DB：**Docker Postgres（仅数据库容器化）+ server 用 tsx watch 热重载**。容器 `cch-dev-postgres`（postgres:16，数据卷 `server/.pgdata`，`--restart unless-stopped`），`.env.dev` 为 `DB_PROVIDER=postgres` + `DATABASE_URL`。server 启动：`cd server && HOST=0.0.0.0 npx tsx watch --env-file=.env.dev ./sources/main.ts`。说明：PGlite 无法容器化（它是嵌进 server 进程的 wasm 库，不是独立服务）；PGlite 嵌入式方案曾因 tsx watch 硬杀进程导致数据目录损坏，故数据库放外部 Postgres，热重载随便用。
- 提交：`1f7129c`（PTY 中继主体）、`f40afba`（session 对齐 + 自动 rejoin）。

## 9. 会话管理重设计（2026-08-15 访谈定稿）

**侧边栏 = 只放活跃会话**：running + exited 未移除的（灰色标记，手动 × 移除=归档 active=false）。历史会话不进侧边栏。
**Resume = 全局搜索面板**：跨设备列出所有 claude conversation（设备→项目分组、顶部搜索、单击恢复）。profile 自动带（server 端记忆：该 conversation 上次用的，没有则用全局上次）。
**New = 独立流程**：显式选设备 + 目录（常用目录+手输）+ profile（记住全局上次）。
**profile 记忆存 server**（UserKVStore：`profile:conv:<claudeSessionId>` 和 `profile:last`）。

> 2026-08-20 修订：New/Resume 合并为单一 **Add** 入口（commit 见 git log）。Add modal = Device（始终显示）+ Directory（shell 式前缀补全，完整目录列子目录）+ 会话区（默认显示当前目录最近 5 个会话卡片，点卡片就地恢复；搜索框非空时切换为全设备全局搜索结果）+ Profile（kv 记忆）。原 New/Resume 两个 modal 已删除。

### 协议/接口新增
- daemon RPC `list-all-conversations`：扫 `~/.claude/projects/*/*.jsonl`，每条返回 {claudeSessionId, title, cwd（读 jsonl 里的 cwd 字段，slug 不可逆所以必须读文件）, updatedAt}；按 updatedAt 倒序，上限 100。
- server REST：`GET /v1/sessions?active=true` 过滤；`PATCH /v1/sessions/:id {active?}`（归档用）；`GET/PUT /v1/kv/:key`（account 隔离的字符串 KV，给 profile 记忆用）。
- web：侧边栏只显示 active；New / Resume 拆成两个入口两个 modal。

### 不做
- 手动 folder（会话天然按 cwd 归属，自动分组已覆盖）。
- daemon 会话表持久化（resume 走 tag 对齐已等价）。

## 10. 邀请制注册 + 公网安全加固（2026-08-20，bb88ea1）

- 邀请制：admin 在 /admin 生成邀请链接（expiresInHours 默认 168、maxUses 默认 1），受邀者 /register?token= 注册（邮箱即用户名，密码≥8）。存储 = 新 InviteCode model（prisma db push，**无 migration 文件——standalone/生产部署前必须由人手工补 migration**，否则邀请接口报表不存在）。
- 安全加固：helmet（CSP 暂关）、rate-limit（全局 300/min、/v1/auth/* 和 /v1/invites/* 20/min）、CORS 白名单（ALLOWED_ORIGINS，默认同源）、adminAuth 恒定时间比较。
- 明确不做：开放注册（vaultwarden 式共识）、单二进制 CLI（node-pty 打不进）、蒲公英/Play 分发。
- 打包：CLI `npm i -g <tarball>` 后 ccd/cch 可用；Android 本地签名 release APK（keystore 在 android/keystore.properties，未入库，需备份）。
- 阿里云部署：docker-compose（caddy 自动 HTTPS + server 不暴露端口）+ server/deploy/Caddyfile + DEPLOY_ALIYUN.md 手册。

## 11. 权限批准桥接（2026-08-22，01cb2b4）

claude PreToolUse hook（per-session --settings，timeout 60s）→ hook.js 经 unix socket 问 daemon → daemon 挂起并经 server 中继 perm:request → web 浮动卡片 Allow/Deny → 原路返回。55s 无响应 hook 静默退出 → claude 本地 TUI 正常弹框（双端不互卡）。profile 开 skip_permissions 时不触发。

## 12. 导航布局重设计（2026-08-25 访谈定稿）

**桌面端**：
- 顶栏两行：row1 = logo+版本号 + `+ Device`；row2 = 小图标右对齐（折叠 sidebar、刷新、登出[门形图标]）
- 设置从顶栏移到 sidebar 底部独立一行（"Settings"）
- sidebar 折叠：顶栏图标 + Cmd/Ctrl+B 快捷键，状态存 localStorage

**移动端（Android/iOS/PWA）**：
- 底部 tab 栏：会话(≡ 抽屉) / Add(+) / Settings(⚙)
- 主题切换、刷新、登出全部收进 Settings 页（判定为低频）
- Settings 页内容：改密码、主题切换、登出、版本号

文件：server/user.html + server/user.js
