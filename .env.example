# Required: Master secret for auth token generation. Generate with: openssl rand -hex 32
HANDY_MASTER_SECRET=

# Required for admin UI at /admin. Choose a strong password.
ADMIN_PASSWORD=

# Optional: server port (default 3005)
# PORT=3005
